import React from "react";

import "../styles/sectionContainer.css";

function SectionContainer({ children }) {
  return <div className="ctaContainer">{children}</div>;
}

export default SectionContainer;
